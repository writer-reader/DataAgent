# DataAgent 依赖清单
# 安装: pip install -r requirements.txt

openai>=1.55            # OpenAI 兼容 SDK（base_url 可配置，支持任意中转站）
fastapi>=0.115          # Web 框架
uvicorn[standard]>=0.32 # ASGI 服务器
sse-starlette>=2.1      # Server-Sent Events 流式响应
pydantic>=2.9           # 数据校验（FinalizeReport / 事件模型）
pydantic-settings>=2.6  # 类型安全的 .env 配置加载
PyYAML>=6.0             # 语义层 YAML 解析
sqlglot>=25.0           # SQL AST 解析校验（只放行 SELECT）
pandas>=2.2             # 查询结果 -> CSV
faker>=30.0             # seed 示例数据生成
rich>=13.0              # CLI 表格/高亮渲染

# --- P5 RAG 阶段（可选，初版不装）---
# chromadb>=0.5         # 本地向量库，embedding 走 OpenAI 兼容中转
